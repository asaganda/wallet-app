this will loop each card and show each one (list of cards)
<h1>All the Cards</h1>
<% @cards.each do |card| %>
  <div class="card">
    <ul class="card-attributes">
      <!-- show card type -->
      <li> <%= card.card_type %> </li>
      <!-- show card number -->
      <li> <%= card.number %> </li>
      <!-- show expiration month/year -->
      <li> Expires: <%= "#{card.exiration_month}/#{card.expiration_year}" %> %> </li>
    </ul>
  </div>
<% end %>